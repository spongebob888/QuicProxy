# Linux 如何安装？

基于 Linux 的各种发行版，包括 Ubuntu、OpenWrt 等各种发行版的安装都包括在本章节内。

即使有 gui 的 Linux 发行版，也建议使用脚本一键安装，然后通过 Web 来管理，效果逻辑和原生运行是一模一样的。

## 直接通过一键脚本安装

比如说 Ubuntu、Debian、OpenWrt 等等。

直接运行一键脚本，会自动检测类型然后全自动安装，强烈推荐。

然后就可以通过命令来控制运行：

安装完成之后，直接通过浏览器打开网址来 ui 控制界面。
